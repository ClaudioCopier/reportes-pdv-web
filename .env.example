# Sin prefijo VITE_ = server-only (sólo la función serverless /api/entrar las
# ve, nunca se bundlean al JavaScript público del navegador). Ver SERVIDOR.md.
SUPABASE_URL=
SUPABASE_ANON_KEY=
REPORTES_CLAVE=
